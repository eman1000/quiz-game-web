import { connect } from "react-redux";
import GameChest from "../components/GameChest";

import {
} from "../module";

const mapStateToProps = (state) => ({
});

const mapDispatchToProps = {
};
export default connect(mapStateToProps, mapDispatchToProps)(GameChest);